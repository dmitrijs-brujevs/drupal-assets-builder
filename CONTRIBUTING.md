# Contributing

Use Node.js 22.14 or newer and npm 11.5.1 or newer.

```sh
npm ci
npm run check
```

Keep changes focused, add tests for behavior changes, and do not commit fixture `dist` directories,
`node_modules`, credentials, or npm tokens. Pull requests should explain the Drupal use case and any
effect on output paths or compatibility.
